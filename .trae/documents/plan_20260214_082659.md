在 `ipcManager.js` 的 `setupUtilityEvents()` 方法中添加 `open-config-folder` 监听器：

1. 在文件顶部导入 `ScheduleConfigExtractor` 类
2. 在 `setupUtilityEvents()` 方法末尾添加 `ipcMain.on('open-config-folder', ...)` 监听器
3. 使用 `shell.openPath()` 打开配置文件所在目录

配置目录路径：`%APPDATA%/electron_class_schedule_x/config/`